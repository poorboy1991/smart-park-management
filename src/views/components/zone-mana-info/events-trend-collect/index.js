import React, {Component} from 'react'
import style from './index.less'

export default class eventsTrendCollect extends Component {
    render() {
        return (
            <div className={`${style.aa}`}>
                事件发生趋势分析
            </div>
        )
    }
}