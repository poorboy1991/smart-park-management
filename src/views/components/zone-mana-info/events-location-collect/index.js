import React, {Component} from 'react'
import style from './index.less'

export default class eventsLocationCollect extends Component {
    render() {
        return (
            <div className={`${style.aa}`}>
                事件发生场所分析
            </div>
        )
    }
}