import React, {Component} from 'react'
import style from './index.less'

export default class eventsCollect extends Component {
    render() {
        return (
            <div className={`${style.aa}`}>
                事件类型分析
            </div>
        )
    }
}