import React, {Component} from 'react'
import style from './index.less'

export default class GuestPictureInfo extends Component {
    render() {
        return (
            <div className={`${style.aa}`}>
                游客画像概览
            </div>
        )
    }
}